<h1>Editing Book</h1>
<% if @list.errors.any? %>
  <%= @list.errors.count %> error prohibited this book from being saved:
  <% @list.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<%= form_with model:@list, url:"/todolists/#{@list.id}", local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>body</h4>
  <p><%= f.text_area :body %></p>
  <p><%= f.submit 'Update Book' %></p>

  <p><%= link_to 'Show', "/todolists/#{@list.id}" %>|<%= link_to 'back', "/books" %></p>
<% end %>